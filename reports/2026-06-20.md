# AI Radar — daily report 2026-06-20

A quiet day on top of this morning's weekly W25 recalibration. **No new trend
evidence, no stage moves.** The lab sweep, an 8-org open-weight recheck and the
GitHub watch are all unchanged since 06-19; the native-serving gates stay
unfired. The notable operational win is a **source-heal**: the curator's 06-19
allowlist widening took effect, so direct HF / HN endpoints now work.

## Ledger changes

- **No evidence added, no stage moves.** Native-M3/GLM-5.2 vLLM/SGLang support
  and a benchmarked `turboquant` release remain UNFIRED (two weeks) — the standing
  promotion gates for open-weight-003 / subquad-attn-012 / lowbit-quant-011.
- **Watchlist +4 intake ([41](../TRENDS.md#observation_queue)),** all
  metadata-verified via export.arxiv, all below the trend bar:
  1. **From Trainee to Trainer** ([2606.17682](https://arxiv.org/abs/2606.17682),
     5 authors, 06-16) — an *LLM-as-Environment-Engineer* loop where the current
     policy reads its own failure trajectories to redesign the next RL stage's
     environment. A **3rd independent group** on rl-env-005's "co-evolve the
     environment with the policy" sub-theme (after EvoTrainer, GenEnv).
  2. **EfficientRollout** ([2606.18967](https://arxiv.org/abs/2606.18967),
     10 authors, 06-17) — system-aware self-speculative decoding to cut the
     long-tail rollout-latency bottleneck in RL post-training; adjacent to
     rl-env-005 (rollout infra) and pd-disagg-002 (serving efficiency).
  3. **Kairos** ([2606.16533](https://arxiv.org/abs/2606.16533), 24 authors,
     v2 06-15) — a "native world model stack for Physical AI" (world models as
     *operational infrastructure*: persistent long-horizon state, efficient
     deployment). A WATCH-AREA item (world models, per the 06-20 curator scope
     add); off the core LLM axes, surfaced not forced.
  4. **Beyond Static Leaderboards** ([2606.19704](https://arxiv.org/abs/2606.19704),
     61 authors, 06-18) — a coordinated deep-dive (14 parallel studies) of one
     MCP-based industrial-agent benchmark; no single benchmark covers >4–5 of
     the dimensions deployment exposes. Evals + multi-agent-eng-009 / mcp-001.

## Top 3 of the day

1. **Beyond Static Leaderboards** — predictive validity over static scores for
   evaluating LLM agents, the largest coordinated MCP-agent-benchmark study to
   date: https://arxiv.org/abs/2606.19704
2. **From Trainee to Trainer** — a 3rd group has the policy design its own next
   RL environment; the "co-evolve environment ↔ policy" line is consolidating:
   https://arxiv.org/abs/2606.17682
3. **Quiet on releases** — no new frontier open weights since GLM-5.2 (06-16);
   8-org HF recheck flat; vLLM still v0.23.1rc0 (no M3/GLM-5.2-native, no
   benchmarked `turboquant`).

## Study picks

- [Beyond Static Leaderboards](https://arxiv.org/abs/2606.19704) — study it
  before trusting any agent leaderboard: the argument for *predictive validity*
  (does the benchmark predict deployment behavior?) is the practical lesson for
  anyone evaluating or shipping LLM agents.

## Lab sweep, pulse & watch

- **Lab feeds** (RSS/Atom): DeepMind (top still UK house-building / Securing-AI-
  agents 06-16), HF blog (top still MosaicLeaks / Beyond-LoRA / agentic-eval),
  OpenAI (newest 06-18, enterprise spend controls — business), NVIDIA (feed
  `<updated>` 06-16, AR-glasses post off-axis) — nothing new since 06-19.
- **Open-weight recheck** (HF API direct, 8 orgs): no new main LLM drops — GLM-5.2
  (06-16), Kimi-K2.7-Code (06-11), MiniMax-M3, DeepSeek-V4 unchanged; Mistral
  unchanged (July rumor still unconfirmed); XiaomiMiMo MiMo-Audio (audio) /
  NVIDIA GR00T/Cosmos3/Nemotron-NVFP4 off-axis or already cited.
- **GitHub watch** (releases.atom): vLLM v0.23.1rc0, SGLang v0.5.13.post1,
  llama.cpp b9739 (rolling), exllamav3 0.0.43, Dynamo nemotron-ultra-dev tag,
  transformers v5.12.1 — native-serving gates unfired.
- **Community pulse** (HN front page via **direct** Algolia API + tvly news): no
  new earthquake — HN leads with a "GPT-5.5 hallucinates 3× more than GLM-5.2"
  comparison (intake); the Anthropic Fable/Mythos export-control shutdown
  continues (framed as an open-source tailwind — already-logged 06-12 event).
- **Exploration slot** (significance-first per the 06-20 curator steer; HF daily
  papers 06-18/06-19 ranked by upvotes, top items read regardless of topic): the
  four queued items above + top vision item Moebius 0.2B inpainting
  (2606.19195) noted off-axis.

## Source-heal & tooling

- **HEALED:** the curator's 06-19 allowlist widening took effect. Direct
  `huggingface.co/api/daily_papers`, `huggingface.co/api/models` and
  `hn.algolia.com` now return JSON from the routine IP (previously tvly-only) —
  recorded as the preferred method in SOURCES.md. **Reddit-direct still returns
  "Blocked by egress policy"** (site/datacenter-IP block) → tvly fallback kept.
- **Tooling drift:** the `tvly` CLI flag set changed from the `tavily-search`
  skill docs — `--time-range [day|week|month|year]` (not `--days`),
  `--depth` (not `--search-depth`), `--topic [general|news|finance]`. Noted in
  SOURCES.md; a `tavily-search` skill refresh is a candidate for the next weekly.

## Next

- **Serving-stack absorption** of GLM-5.2 / MiniMax-M3 (vLLM/SGLang native,
  DSA/MSA kernels) and a benchmarked vLLM `turboquant` — the standing gates for
  subquad-attn-012 and lowbit-quant-011, unfired for two weeks.
- **Mistral July drop** — recheck the HF org for the rumored open-weight family
  (a 2nd non-Chinese lab on open-weight-003).
- **RL-environment co-evolve** — watch whether automated environment design
  (From Trainee to Trainer / EvoTrainer / GenEnv) reaches a training stack
  (OpenEnv / NeMo Gym); would lift it from a sub-theme.
- **Diffusion LMs** — open DiffusionGemma's artifact + watch for a 3rd
  independent open diffusion base model (the seed gate).
- Now that HF/HN are direct, use the upvote-ranked HF daily-papers read every
  run for the significance-first exploration slot.
