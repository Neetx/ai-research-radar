# AI Radar — daily report 2026-06-14

A focused daily scan after yesterday's two passes. One stage move, one pinned-axis
refresh, two source feeds healed. The headline: the open-weight wave is now
documented being absorbed into the production serving stack within days of release.

## Ledger changes

- **open-weight-003** (Open-weight frontier MoE wave): +1 evidence, **stage
  emerging → accelerating**. NVIDIA's official technical blog (2026-06-12)
  ships a full **MiniMax-M3 deployment guide** across TensorRT-LLM, SGLang,
  vLLM (recipe) and NVIDIA Dynamo (disaggregated serving — 4× interactivity at
  32k ISL on Blackwell), with the SGLang launch command running the MXFP8
  checkpoint (`--quantization mxfp8`). The explicitly-logged "serving-framework
  adoption of M3" watch item fired — an open-weight frontier MoE absorbed into
  the production serving stack within days of release, on top of the ~2-month
  weeks-scale cadence (M2.7 → GLM-5.1/Kimi-K2.6 → DeepSeek-V4 06-08 → M3 06-13).
  Confidence stays medium. **Honest caveat:** vLLM stable v0.23.0 does *not* yet
  natively support M3 ("Minimax M3 is not yet supported in this version" —
  recipe-only); native support is still landing. `last_evidence` unchanged
  (NVIDIA post 06-12 < the 06-13 model card).

- **latent-comm-010** (Latent inter-model communication, *pinned*): +1 evidence,
  **stays emerging**, `last_evidence` 2026-06-05 → **2026-06-11**. A fifth
  independent cluster (NVIDIA/Penn-line, arXiv **2606.13594**, "Dense Latent
  Communication Across Heterogeneous Agents", cs.MA) attacks the part earlier
  C2C-style work side-steps — cross-model latent alignment between *heterogeneous*
  agents (different model families, not duplicate copies) — pushing latent
  communication from steering toward dense semantic transfer. A single preprint,
  so no stage move; it refreshes the pinned axis.

- **Queue:** +2 intake. (1) Open-weight: a Xiaomi-line **MiMo-V2.5-Pro** recurs
  in r/LocalLLaMA value comparisons (unverified — primary card not opened), and
  **Qwen3.5** surfaced as a primary artifact (Qwen HF SAE base models 05-13 +
  vLLM v0.23.0 routing #44700) — verify both HF orgs next run. (2) **VIA-SD**
  (arXiv 2606.12243) speculative decoding with an intra-model slim verifier —
  verified-weak, serving-efficiency signal. The hardware↔low-bit MX sub-theme
  item gained a datapoint: NVIDIA's M3 guide runs MXFP8 directly in the SGLang
  launch command (MX format now in serving instructions, not just distribution).

## Top 3 of the day

1. **MiniMax-M3 serving stack** — NVIDIA documents M3 on TensorRT-LLM/SGLang/
   vLLM/Dynamo (disaggregated, MXFP8): https://developer.nvidia.com/blog/deploy-long-context-reasoning-and-agentic-workflows-with-minimax-m3-on-nvidia-accelerated-infrastructure/
2. **Dense Latent Communication** — heterogeneous cross-model KV-cache alignment,
   the hard part C2C avoided: https://arxiv.org/abs/2606.13594
3. **vLLM v0.23.0 finalized** — heavy KV-connector/disagg work + Qwen3.5 routing,
   but M3 recipe-only and still no benchmarked `turboquant`:
   https://github.com/vllm-project/vllm/releases/tag/v0.23.0

## Study picks

- [Deploy MiniMax-M3 on NVIDIA](https://developer.nvidia.com/blog/deploy-long-context-reasoning-and-agentic-workflows-with-minimax-m3-on-nvidia-accelerated-infrastructure/)
  — the research→production map for long-context serving (disaggregation, MXFP8, MSA).
- [Dense Latent Communication](https://arxiv.org/abs/2606.13594) — where model↔model
  latent interfaces head beyond C2C.

## Lab sweep, heal & watch

- **Lab feeds** (RSS): DeepMind (top still DiffusionGemma 06-10), HF blog
  (freshest olmo-eval 06-12), OpenAI (all business/policy), Qwen HF org (nothing
  new since 05-28) — no new on-axis posts.
- **Source-heal (2):** NVIDIA feed fixed — bare `/blog/feed` is empty; use
  `/blog/feed/` (trailing slash) or `/blog/category/generative-ai/feed/` (which
  surfaced the M3 post). Meta blog confirmed to have **no RSS** (all paths 404,
  no feed `<link>`) → method recorded as `tvly extract … --extract-depth advanced`.
  Anthropic Tavily-extract method re-confirmed. All logged in SOURCES.md.
- **GitHub watch:** vLLM v0.23.0; SGLang v0.5.13; Dynamo v1.2.1 + M3/Nemotron
  dev tags; transformers v5.12.0; exllamav3 0.0.42; llama.cpp b9630.
- **Community pulse** (intake only): r/LocalLLaMA = DeepSeek-V4 architecture
  deep-dives (CSA+HCA), W8A8 local deployment, open-weight value wars (MiMo-V2.5
  queued). Direct Reddit/HF-papers JSON blocked from this IP — sampled via Tavily.

## Next

- Verify MiMo-V2.5-Pro and Qwen3.5 against primary HF model cards — both would
  corroborate open-weight-003's cadence.
- Watch for M3 reaching **native** vLLM/SGLang releases (current support is
  recipe-only) — the next serving-adoption milestone.
- Still pending: vLLM benchmarked `turboquant` (lowbit-quant-011 watch);
  DeepSeek-V4 / GLM-5.1 license pins.
- Exploration rotation: cs.AR is due again (~every 2–3 runs; last 06-13); HF
  daily papers ran today.

---

## Second pass (queue verification) — 2026-06-14

Focused follow-up chasing the "Next" agenda above. Net result: the open-weight
wave broadens from four to **six** Chinese labs, all verified on primary HF cards.

### Ledger changes (2nd pass)

- **open-weight-003**: +3 evidence (now 9 items), **stays accelerating** (already
  moved this morning; one-move-per-day). `last_evidence` unchanged at 2026-06-13
  — all three new items pre-date the M3 card, so this is breadth/backfill, not
  recency:
  - **Xiaomi MiMo-V2.5-Pro** (`huggingface.co/XiaomiMiMo/MiMo-V2.5-Pro`, 05-08):
    1.02T total / 42B active MoE, hybrid attention (10 full + 60 sliding-window,
    128-tok window) + 3-layer MTP, 384 experts (8/tok), 1M context, FP8-mixed.
    Its own card benchmarks Base vs DeepSeek-V4-Pro / Kimi-K2 Base. A
    MiMo-V2.5-Pro-FP4-DFlash variant followed 06-08.
  - **Alibaba Qwen3.5** (`huggingface.co/Qwen/Qwen3.5-397B-A17B` + family, created
    Feb 2026): native-multimodal MoE line — 397B-A17B, 122B-A10B, 35B-A3B plus
    dense 0.8B–27B, FP8/GPTQ-Int4 variants; multi-million downloads (9B = 5.6M).
    The earlier queue note only had the SAE-Res base artifacts; the main weights
    are confirmed.
  - **Moonshot Kimi-K2.7-Code** (`huggingface.co/moonshotai/Kimi-K2.7-Code`,
    06-12): 1T/32B MoE, MLA + MoonViT vision encoder, 256K ctx, coding/agentic
    (Kimi Code Bench v2 62.0 vs K2.6 50.9; MCP Atlas 76.0), shipped with **native
    INT4** quantization. Newer Moonshot drop than the cited K2.6 (05-19).
- **Queue**: the 06-14 open-weight intake item → **RESOLVED/promoted**. The
  hardware↔low-bit MX note gained two datapoints (Kimi INT4, MiMo FP4-DFlash):
  frontier weights increasingly ship *natively* in low-bit/MX formats.
- **Study pick**: added **Kimi-K2.7-Code** — native-INT4 frontier MoE as a lens
  on low-bit-as-default-distribution + agentic design.

### Sweep / watch / explore (2nd pass)

- **Lab feeds** re-checked: no new posts since the morning run (DeepMind, HF
  blog, OpenAI all unchanged).
- **GitHub watch**: vLLM still **v0.23.0** (M3 stays recipe-only, no benchmarked
  `turboquant`), SGLang still v0.5.13 — no new release this pass.
- **Community pulse** (intake only): r/LocalLLaMA open-weight value wars; a
  curated digest flagged Kimi-K2.7-Code → followed to the primary HF card and
  verified (intake-lane working as designed). Direct Reddit/HF JSON still blocked
  from this IP — sampled via Tavily.
- **Exploration slot**: cs.AR recent (via `export.arxiv` API, venue browse) —
  newest items off-axis (spiking NNs, analog/neuromorphic, quantum, FPGA,
  GPU-chiplet GEMM, EEG); the only on-axis low-bit item (TileFuse 2606.11357) is
  already queued. No new axis.

### Next (updated)

- **MiMo-V2.5-Pro / Qwen3.5 / Kimi-K2.7-Code** now verified — watch whether they
  reach native vLLM/SGLang support like M3's recipe path.
- The native-low-bit-distribution sub-pattern (INT4 / FP4 / MXFP8 on frontier
  weights) is approaching trend-bar breadth — re-check next run whether it
  consolidates into its own trend or under lowbit-quant-011.
- Still pending: vLLM benchmarked `turboquant`; DeepSeek-V4 / GLM-5.1 license pins.
