# AI Radar — Daily report 2026-07-23 (W30)

Quiet frontier field: no new frontier open-weight WEIGHTS drop, no earthquake. One
on-axis capture (SLPO → the pinned latent-reasoning axis) and one pre-flagged dormancy
mark. Full check ran on every mandatory lane.

## Ledger changes
- **Evidence added** — latent-reasoning-006 (pinned, emerging) ← **SLPO "Scaling Latent
  Reasoning via a Surrogate Policy"** (arXiv 2607.19691, 07-22, HK PolyU-line): brings
  outcome-reward RL to autoregressive LATENT reasoners via a surrogate policy density over
  latent transitions (trajectory-level credit) + a correctness-supervised variable-horizon
  stopping head — a new independent group giving latent reasoning the RL-scaling interface it
  lacked. last_evidence 07-17 → 07-22; dropped oldest evidence (2604.25917 RecursiveMAS repo
  line, paper retained as anchor). No stage move (pinned, single preprint).
- **Dormancy mark** — parametric-injection-018 seed → **dormant** (last_evidence 07-02 = 21d;
  pre-flagged by W29 + the 07-20/07-22 dailies; nothing on the compile-behavior-into-weights
  axis refreshed it). Not archived (45-day line ~08-16); reactivates on any post-07-02 primary.
- **Queue** — 26 +4 intake → 30, burned 5 stale → **~25 (back at cap)**.
  - Intake: SLAI T-Rex (2607.20145, full-param post-training of DeepSeek-V4 MoE on Ascend
    SuperPOD — non-GPU training systems, off-axis single group); Beyond Euclidean Clipping /
    Riemannian Isometric PO (2607.10169, PPO-Clip exploration-collapse fix — pairs with
    GECPO 2607.16850 as a 2-group heterogeneous "PPO/GRPO exploration-geometry" pair, convergence
    NOT fired); FVAttn (2607.16190, video sparse-attn load-balancing, subquad-adjacent);
    GigaToken (github.com/marcelroed/gigatoken, ~1000× tokenizer, community).
  - Burned (oldest stale, single-group, no clustering): KronQ (2607.07964, Hessian scalar PTQ
    off the vector/trellis axis, 15d), Self-Guided TTT for long-context (2607.09415, 13d),
    Metacognition survey (2607.11881, 10d), LightMem-Ego (2607.11487, consumer-memory app below
    the agent-runtime bar, 10d), Mesh-LLM on iroh (p2p inference, never opened/unverified, 9d).
- **mcp-standard-001** HELD at accelerating — the 07-28 FINAL MCP spec (its reactivation
  datapoint) is 5 days out; the blog index still shows only the RC + SDK-betas + EMA.

## Capture-leak sweep
- **capture-leak: 14 ids checked / 0 missing** (arXiv ids named in reports 07-20..07-22 all
  present in TRENDS.md; bare ids in burndown tombstones are correctly-dropped references).

## Top 3 of the day
1. **SLPO** (2607.19691) — outcome-reward RL for latent reasoners; the training-side complement
   to the looped/recurrent-depth architecture line. Routed as latent-reasoning-006 evidence.
2. **SLAI T-Rex** (2607.20145, top HF paper 28up) — full-parameter post-training of a
   trillion-param DeepSeek-V4 MoE on Huawei Ascend SuperPOD; a rare non-NVIDIA frontier
   training-systems report. Queued.
3. **GigaToken** (github.com/marcelroed/gigatoken, HN 427) — a ~1000× faster Rust LLM tokenizer;
   a practical systems artifact for tokenization-bound data/serving pipelines. Queued + study pick.

## Study picks
- **SLPO** (2607.19691) — the surrogate-density trick for latent credit assignment + the learned
  stopping interface for latent test-time scaling; the RL-side reference for latent-reasoning-006.
- **GigaToken** (github.com/marcelroed/gigatoken) — Rust pretokenizer/regex engineering + the
  author's optimization writeups; study for the systems, not a research claim.

## Next
- **07-28 FINAL MCP spec** lands mid-week — sweep blog.modelcontextprotocol.io + repo, capture the
  final-spec primary (mcp-standard-001 accelerating/HIGH; a confirm-or-mainstreaming datapoint). If
  07-28 passes with no spec primary, mark mcp-standard-001 dormant.
- **Weekly (W30) tier-(ii)**: OPEN the AI2 posts flagged today — EMO (MoE-for-modularity) +
  DiScoFormer (density/score transformer) — to avoid a capture-lag like the MCP-blog one.
- Watch for a 3rd independent group on the PPO/GRPO exploration-geometry pair (would seed) and on
  non-GPU frontier training-systems (T-Rex).
- Re-test GitHub external scope each run (still 403).
