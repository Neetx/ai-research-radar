# AI Radar — Daily report 2026-06-24

First run of the day (full check). State loaded; HEAD = origin/main = the 06-23
commit. Orphaned-branch check: `claude/serene-pasteur-iys398` is at 06-19 (behind
main) — no state to recover. Tooling note: tvly auth was RESTORED this session
(`pip install tavily-cli` fixed the 06-23 "invalid API key" failure), so the
Reddit/tvly lanes work again.

## Ledger changes
- **agent-eval-014 (deployment-grounded agent eval)** — added evidence
  **NatureBench** (arXiv 2606.24530, 06-23, 17 authors; #2 HF daily paper today,
  26 up): a benchmark tasking coding agents with REPRODUCING the published SOTA of
  Nature-family papers — a real-task, long-horizon agent eval (scientific-code
  reproduction), the fifth independent group on this axis within a week. Single
  new primary on the existing axis → evidence add (last_evidence 06-22 → 06-23);
  **no stage move** (still seed; the convergence remains on the critique, not a
  shared metric).
- **observation_queue** — 3 captures, 2 drops (cap-bounded burndown):
  - +Qwen-AgentWorld (world-model watch-area capture + study pick; see below)
  - +AOHP (2606.23449) and +OpenRath (2606.19409) → the agent-harness/runtime-infra
    cluster note; AOHP makes harness-as-object 3 datapoints (HarnessX/Xcientist/AOHP)
    but the claims are heterogeneous, so **convergence did not cleanly fire** —
    flagged for weekly W26 to assess "agent runtime/harness/memory as engineered
    infrastructure" (now 6+ groups across 3 distinct mechanisms) as a candidate axis.
  - +exploration note (below-bar/off-axis 06-24 batch).
  - DROPPED 2 stale isolated 06-17 items: the NVIDIA low-precision-training blog
    (single-vendor engineering post, 7d no recurrence) and the cs.DC serving cluster
    note (RouteBalance/LUMEN/Tangram — single-group serving works, 7d, no engine
    adoption).
- No stage moves; no new trend seeded.

## Top 3 of the day
1. **Qwen-AgentWorld-35B-A3B** — Alibaba/Qwen ships open weights for a "native
   language world model" (paper 2606.24597, #1 HF daily paper): predicting the next
   environment state given an agent's action+history IS the training objective from
   the CPT stage onward, across seven agent-interaction domains —
   https://huggingface.co/Qwen/Qwen-AgentWorld-35B-A3B
2. **NatureBench** (2606.24530) — can coding agents match the published SOTA of
   Nature-family papers? A deployment-grounded scientific-reproduction benchmark →
   https://arxiv.org/abs/2606.24530
3. **AOHP** (2606.23449) — an open-source OS-level agent harness (personalized,
   efficient, secure), a 3rd harness-as-engineered-object datapoint →
   https://arxiv.org/abs/2606.23449

## Study picks
- **Qwen-AgentWorld (35B-A3B language world model)** — a concrete, runnable bridge
  between world models and agent infrastructure: a *learned simulator of agentic
  environments* (adjacent to the verifiable-RL-environments line) rather than a
  video/physics world model, with the next-state-prediction objective baked in from
  pretraining — study the three-stage CPT→SFT→RL recipe and AgentWorldBench.

## Next
- **Watch-area pulse on world models is rising** (Kairos / LoopWM / Qwen-AgentWorld /
  World-Action-Models survey = 4 distinct groups in ~10 days). Per curator scope
  these stay watch-only (not a tracked axis), but the recurrence is a candidate new
  axis for a weekly to weigh — surface, don't force.
- **Weekly W26 should decide** whether to seed an "agent runtime/harness/memory
  infrastructure" axis: harness-as-object (HarnessX/Xcientist/AOHP) + self-evolving
  memory (FinAcumen/MAA) + runtime-state (OpenRath) now span 6+ groups, but no single
  mechanism yet has ≥3 on the same specific claim.
- **Standing gates still unfired (2+ weeks):** native vLLM/SGLang serving of
  MiniMax-M3 / GLM-5.2 (open-weight-003, subquad-attn-012 promotion gate) and a
  benchmarked vLLM `turboquant` release (lowbit-quant-011); diffusion-lm-013 still
  one independent open base model short of emerging.
- **Degraded lanes to re-heal:** YouTube curator (feeds/videos.xml 404 persistent
  from this datacenter IP — likely rate-limit; re-resolve channel_ids or try a tvly
  extract of /videos next run) and news.smol.ai/AINews (JS-only index, extract
  returns footer). Re-check Mistral HF org for the rumoured July open-weight family.
