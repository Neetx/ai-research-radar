# AI Radar — daily report 2026-06-13

First daily run after the W24 weekly recalibration, and the first run of the
mandatory lab-sweep + GitHub-watch + community-pulse stack. Two ledger trends
moved/strengthened; the longest-standing queue item was resolved against primary
sources.

## Ledger changes

- **rl-env-005** (Verifiable RL environments): +1 evidence, **stage emerging →
  accelerating**. The "standard interface between environments and trainers"
  watch item fired: Meta-PyTorch + Hugging Face launched **OpenEnv** (HF blog,
  2026-06-08) — a common `step()`/`reset()`/`close()` spec (0.1 RFC at
  github.com/meta-pytorch/OpenEnv) and a shared Hub, with integrations across at
  least four independent training stacks (TRL — docs live, Unsloth, verl, SkyRL)
  plus Meta's TorchForge. That is the cross-framework production adoption the
  promotion gate requires, on top of the existing market/product evidence
  (Bugcrowd, Prime Intellect, NVIDIA NeMo Gym). `last_evidence` unchanged
  (OpenEnv 2026-06-08 < the 06-10 survey/RACES items).

- **agent-security-004** (Agent security): +3 evidence, **stays dormant**. The
  production agent-vuln cluster is now verified and promoted from the queue:
  **EchoLeak** (CVE-2025-32711, M365 Copilot, CVSS 9.3, NVD 2025-06-11 + AAAI
  paper arXiv 2509.10540), **Reprompt** (CVE-2026-24307, M365 Copilot info
  disclosure, CVSS 9.3, 2026-01-22), and a **Microsoft Security advisory**
  ("prompts become shells", 2026-05-07) on an RCE class in agent frameworks
  (LangChain CVE-2026-34070). Three independent disclosers (Aim Labs, Microsoft,
  LangChain). All items are dated ≤2026-05-07 — before the current last_evidence
  (2026-05-17) — so the dormant flag is unchanged: it reflects the 21-day
  recency rule mechanically, not the trend's real-world health.

- **Queue:** −1 resolved (EchoLeak/GeminiJack/Reprompt → promoted; GeminiJack
  kept as an intake note, vendor-disclosed by Noma Security but no NVD CVE
  found). MiniMax-M3 item refreshed: NVIDIA Dynamo cut a `v1.3.0-minimax-m3-dev.1`
  pre-release tag (2026-06-12) referencing M3 — a serving-stack corroboration,
  but still no open weights on the MiniMaxAI HF org. +2 new signals: DiffusionGemma
  (open diffusion text model, single-org); cs.CV exploration (nothing met the bar).

## Top 3 of the day

1. **OpenEnv** — Meta-PyTorch + Hugging Face standardise the agentic-environment
   interface (step/reset/close + Hub), adopted across TRL/Unsloth/verl/SkyRL:
   https://huggingface.co/blog/openenv
2. **EchoLeak verified** — the first real-world zero-click prompt injection in a
   production LLM (M365 Copilot, CVE-2025-32711, CVSS 9.3), AAAI 2025:
   https://arxiv.org/abs/2509.10540
3. **DiffusionGemma** — Google ships an open diffusion-based text model claiming
   4× faster generation; watch whether diffusion LMs become a recurring axis:
   https://deepmind.google/blog/diffusiongemma-4x-faster-text-generation/

## Study picks

- [OpenEnv](https://huggingface.co/blog/openenv) — the spec to read to see where
  agent-training infrastructure is converging.
- [EchoLeak (AAAI 2025)](https://arxiv.org/abs/2509.10540) — the canonical
  production exploit behind the "security must move into the architecture" thesis.

## Lab sweep & watch (firsts)

- Lab feeds: DeepMind (deepmind.google/blog/rss.xml works), HF blog, OpenAI
  (all business/policy this week) swept. **Feed fixes logged in SOURCES.md**:
  Anthropic has no public RSS (404), the Meta blog feed returned empty, and the
  Qwen RSS is stale (newest Sep 2025).
- GitHub watch: vLLM **v0.23.0** (Gemma-4 encoder-free, DeepSeek-V4 hardening,
  disagg PP-aware handshake — but **no benchmarked `turboquant`**, so the
  lowbit-quant watch item has not fired); SGLang v0.5.13; exllamav3 0.0.42;
  Dynamo M3/Nemotron-Ultra dev tags.
- Community pulse: r/LocalLLaMA = DeepSeek-V4 + Gemma/Qwen KV-cache quant buzz
  (intake only). HN sampling degraded (Tavily JSON error).

## Next

- Watch for a MiniMax-M3 open-weight drop (Dynamo dev tag suggests imminent) and
  pin licenses on DeepSeek-V4/GLM-5.1 model cards — open-weight-003 is least fresh.
- Track whether NVIDIA NeMo Gym or a Chinese lab adopts OpenEnv vs. its own stack;
  and whether vLLM `turboquant` reaches a benchmarked release.
- cs.AR rotation is due (~every 2–3 runs; last 2026-06-12) — run it next.
- Fix the Anthropic/Meta/Qwen feed URLs flagged in SOURCES.md on the next sweep.
</content>
