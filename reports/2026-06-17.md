# AI Radar — daily report 2026-06-17

A genuinely new open-weight drop, not backfill: **Z.ai shipped GLM-5.2** open
weights (MIT), the first GLM line with a full 1M-token context. That refreshes
the open-weight trend's recency and reaffirms the weeks-scale cadence on *recent*
releases specifically. A second find — **LoopCoder-v2** — refreshes the pinned
looped/recursive-reasoning axis.

## Ledger changes

- **open-weight-003** (Open-weight frontier MoE wave): +1 evidence (GLM-5.1 line
  replaced by GLM-5.2), **stage unchanged** (🚀 accelerating), `last_evidence`
  **2026-06-13 → 2026-06-16**.
  - **GLM-5.2** (added) — [HF card](https://huggingface.co/zai-org/GLM-5.2),
    createdAt 06-16, **MIT**, 0 downloads (fresh): ~753B-total MoE (safetensors
    753.3B), architecture `GlmMoeDsa` (DeepSeek-style **sparse attention** — the
    "index share" trick), Z.ai's new flagship and the **first GLM at 1M context**;
    the card benchmarks openly against GLM-5.1 / Qwen3.7-Max / MiniMax-M3 /
    DeepSeek-V4-Pro and the closed frontier (Claude Opus 4.8 / GPT-5.5 / Gemini
    3.1 Pro), with broad gains over GLM-5.1 (SWE-bench Pro 62.1 vs 58.4, HLE 40.5
    vs 31). A **GLM-5.2-FP8** sibling shipped alongside. Verified against the
    primary HF card + safetensors/config API metadata. Supersedes the GLM-5.1
    (754B, 05-13) line — within-lab cadence GLM-5.1 → GLM-5.2.
  - Cadence on recent releases: DeepSeek-V4 06-08 → MiniMax-M3 06-13 →
    GLM-5.2 06-16.

- **latent-reasoning-006** (⭐ Latent/recursive reasoning): +1 evidence,
  **stage unchanged** (📈 emerging), `last_evidence` **2026-06-04 → 2026-06-16**.
  - **LoopCoder-v2** (added) — [arXiv 2606.18023](https://arxiv.org/abs/2606.18023),
    19 authors, 06-16: a 7B **Parallel Loop Transformer** coder family trained
    from scratch (18T tokens) at different loop counts, studying loop-count via a
    gain–cost view (extra-loop refinement vs cross-loop positional mismatch); the
    two-loop variant beats the non-looped baseline across code gen, code
    reasoning, agentic SWE and tool-use. Metadata via export.arxiv. An
    independent group extending the looped-transformer line (LT2/Ouro/PoLar) with
    an actually-trained model.

- **Queue:** +4 intake, no promotions/drops. (1) A forming
  **sparse-attention-for-cheap-long-context** sub-theme — GLM-5.2 DSA + MiniMax
  MSA + DeepSeek-V4 sparse attention (≥3 labs), distinct from the
  hybrid-backbone note. (2) NVIDIA **low-precision-training** microbenchmarks
  (realized FP4 only 1.46–1.66× over MXFP8, short of the 2–3× spec). (3)
  **native-low-bit distribution** broadening (GLM-5.2-FP8, Mistral NVFP4). (4) a
  cs.DC **LLM-serving** cluster (RouteBalance, LUMEN, Tangram-GPU-heterogeneity,
  NPU latency prediction, RISE).

## Top 3 of the day

1. **GLM-5.2** — Z.ai's new flagship open MoE (~753B, MIT), first GLM at 1M
   context via a sparse-attention operator: https://huggingface.co/zai-org/GLM-5.2
2. **LoopCoder-v2** — a 7B looped-transformer coder family trained from scratch;
   loop count as a design knob: https://arxiv.org/abs/2606.18023
3. **Sparse attention for cheap long context** — GLM-5.2's DSA joins MiniMax MSA
   and DeepSeek-V4 sparse attention; ≥3 labs ship a sparse-attention operator
   specifically to make 1M-token context affordable in frontier open MoE.

## Study picks

- [GLM-5.2](https://huggingface.co/zai-org/GLM-5.2) — frontier open MoE with 1M
  context via `GlmMoeDsa` sparse attention; study the design + open benchmark
  suite (vs the closed frontier).
- [LoopCoder-v2](https://arxiv.org/abs/2606.18023) — "looped transformers,
  trained not bolted-on"; why two loops tends to be the sweet spot.

## Lab sweep, pulse & watch

- **Lab feeds** (RSS/Atom): DeepMind (top = UK house-building 06-16, off-axis),
  HF blog (olmo-eval 06-12, nothing newer), OpenAI (all business/policy; a
  "predict-model-behavior-by-simulating-deployment" research post 06-16 skimmed,
  off the core axes). NVIDIA blog had a new on-axis 06-16 post — low-precision
  training microbenchmarks (opened → queued); MLPerf 6.0 post skipped (marketing).
  Mistral news index is the existing lineup (Small 4 = 119B, from January);
  z.ai/blog returns 404 (the HF org is the authoritative Z.ai release surface).
- **GitHub watch:** vLLM v0.23.1rc0 (Dockerfile bugfix — still no M3-native, no
  benchmarked `turboquant`), SGLang v0.5.13.post1, llama.cpp b9672, exllamav3
  0.0.43, transformers v5.12.1; Dynamo unchanged.
- **Community pulse** (intake only): GLM-5.2 dominates the open-weight
  conversation (r/LocalLLaMA, YouTube, AINews); corroborates the primary HF card.
  Direct Reddit/HN JSON blocked from this IP → routed via tvly.
- **Exploration slot:** arXiv cs.DC recent (venue browse, not cs.AR which ran
  06-16) — a dense LLM-serving/distributed-inference cluster (queued, below bar);
  HF daily papers 2026-06-17 also browsed → LoopCoder-v2 verified and promoted.

## Next

- Watch **serving-stack absorption** of GLM-5.2 (vLLM/SGLang native support,
  DSA/sparse-attention kernels) — the usual promotion gate; and the GLM-5.2 tech
  report/paper (linked from the card, not yet opened).
- The **sparse-attention-for-long-context** sub-theme is at ≥3 labs on model
  cards — watch for first-class serving-engine kernels (MSA/DSA) before promoting.
- Still pending: M3 native vLLM/SGLang; vLLM benchmarked `turboquant`; license
  pins on DeepSeek-V4 / Nemotron; whether the native-low-bit-distribution note
  consolidates to a single claim+artifact bar under lowbit-quant-011.
</content>
