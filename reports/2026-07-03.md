# Daily report — 2026-07-03

Single full-check run across all mandatory lanes. A **quiet field day** (no new
frontier open-weight drop, no earthquake, no ban/policy event) that was
nonetheless substantive on the ledger: the **exploration slot** (HF daily papers
+ a rotating cs.DC browse) produced **one new seed trend** and **three genuine
evidence-adds**. No orphaned `claude/*` state to recover (local HEAD =
origin/main = c56b527; the older `claude/magical-turing-oxwvjf` points at bdc1148,
already an ancestor of main). GitHub remains proxy-scoped to only this repo, so
the GitHub-watch lane was routed via tvly/HF (unchanged from 07-02).

## Ledger changes
- **NEW SEED TREND parametric-injection-018 — "Parametric injection: compiling
  behavior and knowledge into model weights instead of prompt/context".** The
  cluster the 06-20 curator steer + 06-22 queue lines pre-registered ("watch for a
  3rd group → would seed a 'parametric injection vs context' axis") crossed the
  bar: **three independent groups** now share the claim *compile behavior/knowledge
  into compact parameters (adapter/MoE) for a frozen base, instead of keeping it in
  the prompt/context/API* — [DMoE](https://arxiv.org/abs/2606.14243) (Tsinghua;
  knowledge→decoupled-MoE), [Skill-to-LoRA](https://arxiv.org/abs/2606.16769)
  (CUHK; SKILL.md→LoRA adapter), and the new
  [Program-as-Weights](https://arxiv.org/abs/2607.02512) (Harvard; NL-spec fuzzy
  function→PEFT adapter + released FuzzyBench). All three abstracts opened this
  session; the two 06-22 queue lines were folded OUT into evidence. Seed /
  confidence LOW (young, preprints, thematic convergence — heterogeneous in what
  is injected and in mechanism).
- **pd-disagg-002 — evidence add.** [ELDR](https://arxiv.org/abs/2607.00466)
  (07-01, cs.DC) — expert-locality-aware decode routing for PD-disaggregated MoE
  serving; a single new independent primary on the existing axis → evidence
  (last_evidence 06-29 → 07-01). No stage move (accelerating).
- **on-policy-distill-016 — evidence add.**
  ["Denser ≠ Better"/SDPO](https://arxiv.org/abs/2607.01763) (07-02) — a LIMITS
  result (self-distillation policy optimization forgets/collapses out-of-distribution
  in continual post-training). Evidence add (last_evidence 06-29 → 07-02); no stage
  move — a healthy counterweight that keeps the young seed honest.
- **agent-eval-014 — evidence add.**
  ["Are Performance-Optimization Benchmarks Reliably Measuring Coding Agents?"](https://arxiv.org/abs/2607.01211)
  (07-01, David-Lo/SMU) — a benchmark-validity audit of GSO/SWE-Perf/SWE-fficiency;
  9th independent group, closes the 07-02 "open next run" follow-up. Backfill
  (last_evidence stays 07-02); no stage move (still seed, convergence on the critique).
- **observation_queue net −1.** Added the 07-03 line + Morphing-into-Hybrid-Attention
  (2606.30562, subquad-attn-012 conversion-recipe corroboration — that trend is at
  the 10-evidence cap); promoted the two 06-22 lines out. Under the ~25 cap.

## Capture-leak sweep
capture-leak: 37 ids checked / 0 missing — every arXiv id named in reports
06-30..07-02 is present in TRENDS.md, and today's new primaries were routed before
logging (ELDR/Denser≠Better/Are-Perf-Opt → evidence; PAW/DMoE/Skill-to-LoRA →
parametric-injection-018 evidence; Morphing-into-Hybrid → queue).

## Top 3 of the day
1. **Parametric injection crosses the bar** — DMoE / Skill-to-LoRA / Program-as-Weights
   converge that behavior and knowledge can be COMPILED into weights (adapters/MoE)
   for a frozen base as an alternative to RAG/harness-context → seeded 018:
   https://arxiv.org/abs/2607.02512
2. **ELDR** — the PD-disaggregation front adds an MoE-aware angle: route decode by
   EXPERT LOCALITY, not just load, because each decode step loads every expert its
   batch activates: https://arxiv.org/abs/2607.00466
3. **"Denser ≠ Better"** — a rare honest limits result on the OPD hype: dense
   self-distillation forgets and collapses out-of-distribution:
   https://arxiv.org/abs/2607.01763

## Study picks
- **Program-as-Weights (PAW)** — study it as the practical face of the new
  parametric-injection axis: compile a fuzzy function (log-alerting, JSON-repair,
  intent-ranking) from a natural-language spec into a local adapter for a frozen
  small model, with a released 10M-example FuzzyBench and compiler recipe — a
  reproducible pattern for replacing LLM-API glue with cheap local artifacts.

## Next
- **parametric-injection-018 promotion gate** — watch for a shared
  behavior→adapter compilation framework/interface, or a serving/agent framework
  shipping a first-class "skill/knowledge → adapter" layer, or ≥3 groups on ONE
  mechanism with a shared benchmark.
- **cs.DC disaggregated-serving front** — several single-group PD-disagg/KV-transfer
  papers this batch (load-aware prefill deflection, Lynx, HYPIC, MosaicKV); watch
  whether any consolidates on a shared KV-transfer/routing interface (would be
  pd-disagg-002 stage signal).
- **GitHub-watch block persists** (403, proxy-scoped) — the weekly should decide
  whether to ask the curator for broader GitHub read scope.
- **MCP 07-28 final spec** — the concrete reactivation event for mcp-standard-001.
- Rotate a non-arXiv exploration venue next run (a lab/framework GitHub org or an
  engineering blog index) per the W25-P1 venue-diversification amendment.
