# AI Radar — Daily report 2026-06-23

First (and full) run of the day. A productive pass: with arXiv out of the
weekend-quiet zone (window advanced past the 06-18 batch), a deployment-grounded
agent-evaluation cluster that had been forming in the queue/shelf for a week
crossed the convergence bar and was seeded as a new trend — the first new trend
in two weeks, directly answering the W25 anchoring warning. Two curator/community
lanes were degraded (logged + healed where possible).

## Ledger changes
- **NEW seed trend `agent-eval-014` — Deployment-grounded agent evaluation**
  (long-horizon, real-session benchmarks beyond static leaderboards). Convergence
  fired: four independent groups + released benchmarks on the same critique-claim —
  [CEO-Bench](https://arxiv.org/abs/2606.18543) (Princeton, 500-day startup,
  06-16), [Beyond Static Leaderboards](https://arxiv.org/abs/2606.19704) (IBM
  61-author thesis on predictive validity, 06-18), [PlanBench-XL](https://arxiv.org/abs/2606.22388)
  (long-horizon planning in large tool ecosystems, 06-21) and
  [EnterpriseClawBench](https://arxiv.org/abs/2606.23654) (agents from real
  workplace sessions, 06-22). CEO-Bench + Beyond-Static folded out of the queue;
  PlanBench-XL + EnterpriseClawBench surfaced together on HF daily papers today.
  Kept at **seed** with an honest caveat: benchmarks proliferate and the
  convergence is on a *critique* (static evals insufficient), not a shared
  metric/method — independence is clear, coherence is thematic.
- **`rl-env-005` (accelerating) +1 evidence:** [CLI-Universe](https://arxiv.org/abs/2606.22883)
  (06-22) — a verifiable *executable-task* synthesis engine for terminal agents,
  squarely on the environment/task-synthesis axis (cf. RACES, EvoTrainer).
  `last_evidence` 06-10 → 06-22; **no stage move** (single new primary on an
  existing axis; already accelerating).
- **observation_queue −1 (burndown):** dropped stale isolated **Tangram**
  (2606.06302, non-uniform KV-cache compression, 06-16) — single group, no
  recurrence or engine adoption in 7 days. Plus today's exploration note
  (Self-Compacting LM Agents, HydraHead, Tapered-LM, Randomized-YaRN — all
  below-bar). Net queue ~neutral after folding two items into the new trend.
- **Convergence check (the agent-self-improvement cluster) did not fire:**
  memory-evolution (FinAcumen, MAA) + harness-as-object (HarnessX, Xcientist) +
  behavior→weights (Skill-to-LoRA, DMoE) still has no single mechanism with ≥3
  groups on the same claim — unchanged, stays queued.

## Top 3 of the day
1. [EnterpriseClawBench](https://arxiv.org/abs/2606.23654) + [PlanBench-XL](https://arxiv.org/abs/2606.22388)
   — two fresh deployment-grounded agent benchmarks (real workplace sessions /
   long-horizon planning) that, with CEO-Bench and the IBM thesis paper, seed the
   new `agent-eval-014` trend.
2. [CLI-Universe](https://arxiv.org/abs/2606.22883) — verifiable executable-task
   synthesis for terminal agents; new evidence on the RL-environments trend.
3. Non-event: the native serving-engine gates (vLLM/SGLang MSA/DSA/linear-attn
   kernels, benchmarked vLLM `turboquant`) remain **unfired for 2+ weeks** across
   open-weight-003 / subquad-attn-012 / lowbit-quant-011 (release feeds identical
   to 06-22).

## Study picks
None today — the new trend's anchor ([Beyond Static Leaderboards](https://arxiv.org/abs/2606.19704))
is already on the shelf (06-20); nothing else opened clears the bar.

## Degraded lanes (logged + heal status)
- **YouTube curator lane:** the `feeds/videos.xml?channel_id=…` Atom endpoint
  404'd intermittently from this IP for code4AI / bycloud / AI-Explained (one
  early Yannic fetch succeeded); channel HTML is JS-rendered (fetch returns only
  the footer) and WebSearch didn't surface uploads. Logged degraded + a
  source-health note added to SOURCES.md (likely transient YT rate-limiting;
  re-resolve channel_ids next run if it persists). The pointer/digest half of the
  curator lane (Interconnects, Simon Willison) **was** swept and is clean.
- **Tavily / Reddit pulse:** `tvly` is installed but returns "Unauthorized:
  invalid API key" despite `TAVILY_API_KEY` being set (key appears expired this
  session) — so the Reddit broad-pulse fallback was down. Web work fell back to
  the built-in fetch/search tools per AGENTS.md; the HN front-page net (direct
  Algolia) covered the earthquake check and is clean.

## Next
- Re-check the YouTube curator feeds early next run; re-resolve channel_ids if
  still 404, and restore `tvly` auth (or note the key needs curator refresh).
- Watch whether `agent-eval-014` consolidates on common metrics / lab-card
  adoption (promotion gate to emerging), and whether a 3rd self-evolving-memory
  group appears (the other queued candidate cluster).
- Native-serving gates remain the standing watch across three trends.
- Recheck Mistral HF org for the rumoured July open-weight family.
</content>
