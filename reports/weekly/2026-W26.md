# AI Radar — weekly recalibration 2026-W26

**Period:** 2026-06-20 → 2026-06-26
**Trends:** 15 total (5 accelerating, 7 emerging, 3 seed, 0 dormant)

State note: `git ls-remote` shows only `main` on the remote; local HEAD = `origin/main`.
No orphaned `claude/*` `radar:` state to recover.

---

## Stage / confidence moves, merges, archivals

| trend | move | reason |
|---|---|---|
| **agent-runtime-015** (NEW) | → 🌱 **seed** | folded the ~2-week-old agent-harness/memory/runtime cluster flagged for W26 by the dailies; harness-as-object trio (HarnessX/Xcientist/AOHP) = 3 same-claim groups clears the bar, + self-evolving-memory (FinAcumen/MAA/agent-native-memory) + runtime-state (OpenRath) = 7 groups, all re-verified via export.arxiv this session |
| open-weight-003 | confidence medium → **high** | 7 independent labs ship frontier open-weight MoE on primary cards, sustained 8+ weeks (stage stays accelerating — serving-absorption gate unfired) |
| rl-env-005 | confidence medium → **high** | OpenEnv cross-framework standard + Bugcrowd/Prime-Intellect products + NVIDIA NeMo Gym + research stream |
| agent-security-004 | confidence medium → **high** | architectural-turn thesis backed by 3 theory groups + DeepMind/Microsoft + 3 production CVEs (stage stays emerging) |

In-week moves already logged by the dailies: **diffusion-lm-013** seed → emerging
(06-25, iLLaDA = 3rd open diffusion base model); **agent-eval-014** NEW seed (06-23,
4-group deployment-grounded-eval cluster). No demotions, no archivals, no dormancy
this week (oldest active `last_evidence` is 06-11 = 16 days < 21).

**Dormancy watch (W27):** mcp-standard-001, pd-disagg-002, agent-sandbox-007,
small-cpu-models-008 (pinned), latent-comm-010 (pinned) all sit at `last_evidence`
06-11 and cross the 21-day line ~07-02 — refresh or honestly mark dormant; consider
accelerating → emerging for mcp/pd-disagg if still quiet.

---

## Queue and study shelf

- **Queue:** PROMOTED & MERGED 7 items into agent-runtime-015 (provenance retained).
  Re-dated/consolidated the >14-day native-low-bit-distribution cluster (06-13/06-16/06-17)
  into one forming note — still below the lowbit-quant-011 bar (a distribution-format
  pattern, not ≥3 orgs on a shared low-bit *method*). Kept as forming: agentic-RL
  reward/supervision cluster (06-26), behavior→weights (DMoE/Skill-to-LoRA), world-models
  (watch-only). Dropped stale isolated EfficientRollout. Live queue ≈18 (under cap).
- **Study shelf:** 19 picks, none > 30 days → **no prune** (per the persist-to-~20 rule).
  No true duplicates; the diffusion / looped clusters are already tracked trends.

---

## Strongest and weakest trend

**Strongest:** diffusion-lm-013 — went seed → emerging on a clean gate (iLLaDA, the
3rd independent open diffusion base model) and ties autoregressive Qwen2.5-7B on avg.
**Weakest:** agent-sandbox-007 — never refreshed since first_observed 06-11; vendor-page
evidence only, now 16 days quiet and dormancy-adjacent.

---

## Forward-looking bets for W27

1. **agent-runtime-015 → emerging** — watch for a shared harness/memory spec (an
   OpenEnv-style standard) or ≥3 groups on one mechanism with a shared API.
2. **agentic-RL reward/supervision cluster** — a 3rd same-claim group ("verification
   is the bottleneck") would seed an axis in tension with rl-env-005's premise.
3. **Serving-engine gates** — vLLM v0.24.0 GA + SGLang ≥v0.5.14 for native MSA/DSA /
   linear-attention kernels or a benchmarked `turboquant` (open-weight-003 / subquad-attn-012 / lowbit-quant-011).
4. **Refresh the quiet pinned axes** — open a Liquid AI LFM artifact (small-cpu-models-008)
   and check latent-comm-010 before 07-02 dormancy.
5. **Monthly hit/miss retrospective due W27** (first weekly of July).

---

## Source strategy changes

Full entry in `strategy_notes` (2026-06-27). **No anchoring warning** (a new trend was
created in-week). **Coverage-honesty finding:** the Phase-1 lab-sweep list is only
partially swept — the low-cadence research-lab blogs + ALL hardware blogs are absent
from every day's rotation log (a coverage lie) → proposed amendment **W26-P1** (tier
into every-run vs weekly-rotation) and the weekly takes the low-cadence sweep.
**YouTube curator** IP-blocked end-to-end (3+ runs) — kept (HF-papers+HN carry the load;
a Reddit/YT API key is the curator fix). Added **GSAI-ML** (LLaDA/iLLaDA) HF org for the
diffusion axis. Discovered-source-candidates staging empty (loop is one run old).

---

## Calibration metrics

```
W26: queue +~17/→~7 (folded to agent-runtime-015)/−~10 · evidence +~8 in-week ·
moves: 1 in-week promotion (diffusion-lm-013) + 1 in-week seed (agent-eval-014) +
at weekly 1 seed (agent-runtime-015) + 3 confidence raises · exploration 7/7
(100%) but venue MONOCULTURE persists · off-axis: NO anchoring warning ·
coverage GAP (low-cadence lab/hardware blogs absent all week → W26-P1) ·
degraded: YouTube curator (IP-blocked) + AINews (JS-only).
15 trends (5 accelerating, 7 emerging, 3 seed, 0 dormant).
```
Monthly hit/miss retrospective: **due W27**, not run this week.

---

## Amendments

### Applied this week (one dedicated commit each)
- **W25-P1** — `routines/daily.md` exploration slot: rotate the exploration VENUE,
  ≥1 non-arXiv/non-HF-papers venue per week. Signal (venue monoculture) persisted in W26.
- **W25-P2** — `routines/daily.md` Step 4: seed a ≥3-org bar-clearing sub-theme the day
  it crosses, don't hold it for adoption. Signal persisted (harness-as-object held 06-24→weekly).

### Acknowledged
- The 06-25 confidence-high-threshold amend (commit `40fb175`) skipped formal cooling
  but matches good judgment; ratified and exercised (the 3 raises above). No rollback.

### Newly proposed (cooling starts 2026-06-27; apply earliest W27 if signal persists; curator may veto via dated strategy_notes entry)
- **W26-P1: Lab-sweep tiering.** Motivating metric: ~12 lower-cadence research-lab +
  hardware blogs listed as "swept every run" were absent from all 7 days' rotation logs.
  Proposed change to `routines/daily.md` lab-sweep bullet + SOURCES.md Phase-1 — split the
  list into **(i) every-run high-cadence** (big labs + the 8 open-weight HF orgs) and
  **(ii) weekly-rotation low-cadence** (AI2, EleutherAI, BAIR, Stanford CRFM, PrismML,
  Cohere, Together, Nous, LMSYS, Liquid AI; hardware: Apple ML, AMD ROCm, Modular,
  Qualcomm), the latter swept by the WEEKLY run + one per day on rotation. Makes the
  registry honest about what is actually checked every run.

### Rollbacks / vetoes
None. (W24-P2 study-shelf prune still healthy; no curator vetoes in `strategy_notes`.)
