# AI Radar — Daily report 2026-07-30

Solid, quieter day: no new frontier open-weight drop, no earthquake, but 5
evidence adds across 4 existing trends — two of them (MirrorCode, OpenAI's
ARC-AGI-3 harness post) are among the sharper agent-evaluation primaries this
ledger has captured. All mandatory lanes checked (lab sweep, HF open-weight
recheck, HN + Reddit pulse, full curator lane, GitHub watch, exploration slot,
tier-ii rotation).

## Ledger changes

- **agent-eval-014** (seed) ← TWO independent primaries: [MirrorCode](https://epoch.ai/mirrorcode)
  (Epoch AI + METR, arXiv 2606.30182) — Claude Opus 4.7 reimplements a
  16k-line codebase in 14h/$251 vs an estimated 2-17 human-weeks; and
  [OpenAI's ARC-AGI-3 harness post](https://openai.com/index/how-two-settings-tripled-our-arc-agi-3-scores) —
  harness choices alone take the same model from 0 to 6/6 levels. Evidence cap
  enforced: dropped PlanBench-XL (06-21) and EnterpriseClawBench (06-22).
- **agentic-rl-credit-017** (seed) ← [CAST](https://arxiv.org/abs/2607.25308) —
  game solvers as turn-level teachers; a 2nd group on the turn-level-credit
  mechanism specifically (with ECHO), the cluster's closest approach yet to
  the ≥3-groups promotion gate.
- **agent-security-004** (emerging) ← [GPT-Red](https://arxiv.org/abs/2607.26115) —
  OpenAI's self-play red-teaming agent trained to discover novel
  prompt-injection attacks, used to adversarially train GPT-5.6. Evidence cap
  enforced: dropped Defense Trilemma (04-07).
- **small-cpu-models-008** (pinned, emerging) ← vLLM's
  ["Optimizing vLLM on Arm CPUs"](https://blog.vllm.ai/blog/2026-07-29-optimizing-vllm-on-arm-cpus) —
  Arm+vLLM+PyTorch+oneDNN+KleidiAI shipping INT8 W8A8/W4A8 kernels and
  substantial Arm Neoverse serving gains. Evidence cap enforced: dropped the
  BitNet (01-15) line.
- No stage moves (all four adds are single independent primaries reinforcing
  existing seed/emerging status, per the daily rule).
- **Queue**: +8 intake (ModelExpress, DecoEvo, SkillRise, RSIBench-Data, the
  "AI agents doing open-ended AI research" case-study paper, OmegaUse-OfficeVal,
  StealthBench, Anthropic's cryptographic-weaknesses post). No forced
  burndown (23 lines, under the ~25 cap); reviewed the two oldest live lines,
  left in place (still-relevant seeding-history content).
- **Dormancy**: full re-enumeration, all 18 trends — none crosses the 21-day
  line today. Already-dormant trends (rl-env-005 / agent-sandbox-007 /
  multi-agent-eng-009, all 06-22 = 38d) approach the 45-day archive line
  (~08-06) — flagged for the next 1-2 weeklies.
- **Convergence**: PPO/GRPO pair still 2 groups; RL-training-system stacks
  still 4 heterogeneous groups; agent-memory-benchmark pair still 2 groups.
  NEW pair: "AI agents doing open-ended AI research" (RSIBench-Data + the
  Kapoor et al. case-study paper) — 2 groups, watch for a 3rd. Turn-level
  credit assignment (ECHO + CAST) — 2 groups on one specific mechanism,
  closest cluster to firing.

## Capture-leak sweep

- **capture-leak: 7 ids checked / 0 missing** (arXiv ids named in
  reports/2026-07-29.md, the only daily report since the last sweep — all
  present as discrete evidence or queue references; re-verified this run).

## Top 3 of the day

1. **MirrorCode** (epoch.ai/mirrorcode) — Epoch AI + METR's week-scale
   program-reimplementation benchmark; Claude Opus 4.7 rebuilt a 16k-line
   codebase in 14 hours for $251 vs an estimated 2-17 human-weeks.
2. **How enabling two settings tripled our scores on ARC-AGI-3** (OpenAI) —
   harness design (reasoning retention + compaction), not the model, is what
   took the score from 0 to 6/6 levels.
3. **GPT-Red** (OpenAI) — a self-play red-teaming agent purpose-built to find
   novel prompt-injection attacks and adversarially train GPT-5.6 against them.

## Study picks

- **Discovering cryptographic weaknesses with Claude** (Anthropic) — Claude
  Mythos Preview autonomously found a genuine mathematical weakness in a
  post-quantum signature scheme and a new round-reduced AES attack; a rare,
  well-documented case of AI doing original cryptographic research.
- **How enabling two settings tripled our scores on ARC-AGI-3** (OpenAI) —
  essential reading before trusting any single-harness agent benchmark number.

## Coverage notes

- Tavily hit its plan usage limit once (on the vLLM Arm-CPU blog extract,
  worked around via direct curl+grep) — the remaining curator-lane sources
  (Papers with Code, emergentmind, Import AI, Addy Osmani, Louis Bouchard)
  were swept via the built-in WebFetch tool instead, so the full mandatory
  list was still covered end-to-end this pass. WebFetch is now a confirmed
  fallback if Tavily is quota-limited again.
- **Papers with Code now 302-redirects to `huggingface.co/papers/trending`** —
  de-facto merged with the already-swept HF-papers lane; SOURCES.md updated,
  no longer a separate fetch.
- **Import AI freshness confirmed**: issue #466 (07-27), resolving the 07-27
  stale-cache flag (#462/06-22). Surfaced MirrorCode.
- Nous Research and Cohere blogs (today's non-arXiv tier-ii rotation pick)
  both proved DEGRADED for coverage (JS marketing shell / product-index page,
  no dated post archive) — logged in SOURCES.md, not healed this run.
- GitHub external access re-confirmed still 403 (proxy-scoped to this repo
  only); worked around via PyPI + the vLLM blog's direct curl/grep method.

## Next

- **Turn-level credit assignment** (agentic-rl-credit-017): 2 groups (ECHO,
  CAST) on one specific mechanism — the closest queue/evidence cluster to a
  promotion gate; watch for a 3rd.
- **"AI agents doing open-ended AI research"**: a fresh 2-group pair
  (RSIBench-Data, the Kapoor et al. case-study paper) — watch for a 3rd
  independent group before the next weekly.
- **MirrorCode / agent-eval-014**: now 12+ independent groups on the
  deployment-grounded axis with two of the most rigorous yet (Epoch AI+METR,
  OpenAI's own harness post) — flag for the weekly to reconsider confidence
  (medium→high) even though the promotion gate (shared metric/framework)
  hasn't fired.
- **rl-env-005 / agent-sandbox-007 / multi-agent-eng-009**: approach the
  45-day archive line (~08-06) — the next 1-2 weeklies should refresh or
  archive.
- Recheck Nous Research / Cohere with a better-verified URL if the non-arXiv
  rotation returns to them.
